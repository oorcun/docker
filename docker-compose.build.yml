services:
    nginx:
        build:
            context: ./.docker
            dockerfile: nginx.Dockerfile
        container_name: nginx
    php:
        build:
            context: .
            dockerfile: .docker/php.Dockerfile
        container_name: php
